apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gokit-gorilla-enginesvc.name" . }}-test-connection"
  labels:
    {{- include "gokit-gorilla-enginesvc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gokit-gorilla-enginesvc.name" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
